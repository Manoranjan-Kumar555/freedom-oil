import "../pages/already.scss";
import MainLayout from "../layouts/MainLayout";

const ValidQR = () => {
  return (
    <div>
      <MainLayout>
        <div className="already-container">
            <p>
                Please Enter the Valid QR Code
            </p>
        </div>
      </MainLayout>
    </div>
  );
};

export default ValidQR;
